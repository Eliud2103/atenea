import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FarmaciaRegister3Page } from './farmacia-register3.page';

describe('FarmaciaRegister3Page', () => {
  let component: FarmaciaRegister3Page;
  let fixture: ComponentFixture<FarmaciaRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FarmaciaRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
