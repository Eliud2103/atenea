import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HospitalRegister3Page } from './hospital-register3.page';

describe('HospitalRegister3Page', () => {
  let component: HospitalRegister3Page;
  let fixture: ComponentFixture<HospitalRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HospitalRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
