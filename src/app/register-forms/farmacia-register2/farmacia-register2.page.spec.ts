import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FarmaciaRegister2Page } from './farmacia-register2.page';

describe('FarmaciaRegister2Page', () => {
  let component: FarmaciaRegister2Page;
  let fixture: ComponentFixture<FarmaciaRegister2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FarmaciaRegister2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
