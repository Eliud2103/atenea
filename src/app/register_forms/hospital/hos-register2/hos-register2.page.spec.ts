import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HosRegister2Page } from './hos-register2.page';

describe('HosRegister2Page', () => {
  let component: HosRegister2Page;
  let fixture: ComponentFixture<HosRegister2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HosRegister2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
