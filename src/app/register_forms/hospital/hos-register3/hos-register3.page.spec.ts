import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HosRegister3Page } from './hos-register3.page';

describe('HosRegister3Page', () => {
  let component: HosRegister3Page;
  let fixture: ComponentFixture<HosRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HosRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
