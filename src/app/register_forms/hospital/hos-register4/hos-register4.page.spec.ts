import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HosRegister4Page } from './hos-register4.page';

describe('HosRegister4Page', () => {
  let component: HosRegister4Page;
  let fixture: ComponentFixture<HosRegister4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HosRegister4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
