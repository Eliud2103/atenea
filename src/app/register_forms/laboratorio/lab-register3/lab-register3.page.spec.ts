import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LabRegister3Page } from './lab-register3.page';

describe('LabRegister3Page', () => {
  let component: LabRegister3Page;
  let fixture: ComponentFixture<LabRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LabRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
