import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LabRegister4Page } from './lab-register4.page';

describe('LabRegister4Page', () => {
  let component: LabRegister4Page;
  let fixture: ComponentFixture<LabRegister4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LabRegister4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
