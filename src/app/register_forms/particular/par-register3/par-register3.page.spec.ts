import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParRegister3Page } from './par-register3.page';

describe('ParRegister3Page', () => {
  let component: ParRegister3Page;
  let fixture: ComponentFixture<ParRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
