import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParRegister2Page } from './par-register2.page';

describe('ParRegister2Page', () => {
  let component: ParRegister2Page;
  let fixture: ComponentFixture<ParRegister2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParRegister2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
