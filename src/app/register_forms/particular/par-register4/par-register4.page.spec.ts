import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParRegister4Page } from './par-register4.page';

describe('ParRegister4Page', () => {
  let component: ParRegister4Page;
  let fixture: ComponentFixture<ParRegister4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParRegister4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
